## Packages
recharts | For financial analytics charts
framer-motion | For smooth page transitions and card animations
date-fns | For date formatting in reports
clsx | For conditional class names (usually in base stack but good to ensure)
tailwind-merge | For merging tailwind classes

## Notes
The application requires RTL (Right-to-Left) layout support for Arabic interface.
Integration with Replit Auth is active.
AI Insights are fetched from the backend.
